-- any sql that is to be pre-loaded before testsuite runs goes in here
-- statements must be a single line without returns
DROP TABLE PRODUCT IF EXISTS
DROP TABLE LINEITEM IF EXISTS
CREATE TABLE PRODUCT (ID INTEGER NOT NULL AUTO_INCREMENT, PRODUCTCODE VARCHAR, PRODUCTDESCRIPTION VARCHAR, PRODUCTNAME VARCHAR, PRODUCTPRICE DOUBLE, VERSION INTEGER,CREATED_DATE TIMESTAMP, UPDATED_DATE TIMESTAMP, CONSTRAINT CONSTRAINT_18 PRIMARY KEY (ID));

CREATE TABLE LINEITEM (ID INTEGER IDENTITY NOT NULL, QUANTITY INTEGER, SUBTOTAL FLOAT, VERSION INTEGER, CREATED_DATE DATETIME, UPDATED_DATE DATETIME, CART_ID INTEGER, PRODUCT_ID INTEGER, PRIMARY KEY (ID), CONSTRAINT FK_LINEITEM_PRODUCT_ID FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT(ID) ON DELETE RESTRICT ON UPDATE RESTRICT)

