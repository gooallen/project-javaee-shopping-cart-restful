-- any sql that is to be pre-loaded before testsuite runs goes in here
-- statements must be a single line without returns
DROP TABLE CART IF EXISTS
DROP TABLE LINEITEM IF EXISTS
CREATE TABLE CART (ID INTEGER IDENTITY NOT NULL, TOTAL FLOAT, VERSION INTEGER, CREATED_DATE DATETIME, UPDATED_DATE DATETIME, MEMBER_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE LINEITEM (ID INTEGER IDENTITY NOT NULL, QUANTITY INTEGER, SUBTOTAL FLOAT, VERSION INTEGER, CREATED_DATE DATETIME, UPDATED_DATE DATETIME, CART_ID INTEGER, PRODUCT_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE LINEITEM ADD CONSTRAINT FK_LINEITEM_OWNING_CART_ID FOREIGN KEY (CART_ID) REFERENCES CART (ID)
